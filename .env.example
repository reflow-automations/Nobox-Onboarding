# Supabase — project "Orakel chat" (gedeeld met meta-ads-dashboard)
NEXT_PUBLIC_SUPABASE_URL="https://djarvwzvbxlcnxkxczpc.supabase.co"
# Service-role key — server-only, BYPASS RLS, NOOIT exposen aan browser.
# Haal op via: Supabase dashboard → project settings → API → service_role key.
# Zelfde key als meta-ads-dashboard gebruikt — copy uit dat .env.local
SUPABASE_SERVICE_ROLE_KEY=""

# n8n webhook — trigger voor downstream branches (Drive/mail/ClickUp).
# Form-data zit NIET meer in deze payload — alleen { intake_id, reference_id }.
N8N_WEBHOOK_URL="https://n8n.reflowautomations.nl/webhook/nobox-onboarding-intake"

# Bearer token (matched in n8n Code-node "Verify bearer token")
# Rotation: edit zowel hier als de Code-node tegelijk
N8N_WEBHOOK_TOKEN="dHjp17uP69tNWcMZzvLBq5T0ARGbKgyVYDnoXJfC"
